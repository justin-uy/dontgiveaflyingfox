const React = require('react');
const ReactDOM = require('react-dom');
const HomePage = require('../../../shared/components/jane/HomePage.react');

ReactDOM.render(
  <HomePage />,
  document.getElementById('mount')
);
